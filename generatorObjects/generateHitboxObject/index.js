function generateHibox(startingPosition, size, formObject, type) {
    switch (type) {
        case "player":
            ;
        case "drone":

            ;
        case "shell":

            ;
        case "building":

            ;
        case "black_hole":

            ;
        case "star":

            ;
        case "planet":

            ;
        case "wormhole":

            ;
    }
}

export default generateHibox;